<?php
/**
 * A placeholder view to output content based on a given tag
 *
 * @uses $vars['tag']  the tag being viewed right now
 * @uses $vars[entity] the TagDefinition for the given tag (if any)
 */
